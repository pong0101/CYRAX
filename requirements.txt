# Core runtime
open-interpreter==0.4.3
# Pin a wheel-backed LiteLLM release so Windows/Python 3.12 does not
# fall back to compiling an older source distribution with Rust/Cargo.
litellm==1.97.0
ollama==0.6.2
PyYAML==6.0.3
